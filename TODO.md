# TODO

1. Automate testing the web → mobile and mobile → web resume experience, rather than relying on
   manual verification (see the two unchecked manual steps in [PR #1](https://github.com/EliRobinson/gametime-interview/pull/1)'s test plan).
2. Add a script to run all servers (`api`, `web`, `mobile-web`) at once for local development.
