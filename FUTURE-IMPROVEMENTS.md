# Future Improvements

1. Shared components between `apps/web` and `apps/mobile-web` (currently no shared UI layer
   between the two surfaces).
2. Const enums for session/status states, shared between the mobile and web codebases instead
   of each maintaining its own copy.
